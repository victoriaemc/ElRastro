import React from 'react';
import ChatContainer from '../components/ChatContainer';

const ChatPage = () => {
    return (
        <div>
            <h1>Chat Page</h1>
            <ChatContainer />
        </div>
    );
};

export default ChatPage;
